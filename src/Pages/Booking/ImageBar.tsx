import { Image } from 'antd';
import bookingSvg from '../../Img1/booking.svg';

const ImageBar = () => {
    return (
        <Image
            src={bookingSvg}
        />
    );
}

export default ImageBar;
